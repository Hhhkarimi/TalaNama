# مشارکت در طلانما

از مشارکت شما استقبال می‌شود.

## روند پیشنهادی

1. یک Issue برای تغییر موردنظر ایجاد کنید.
2. مخزن را Fork و یک branch کوتاه و توصیفی بسازید.
3. وابستگی‌ها را با **npm ci** نصب کنید.
4. تغییر را پیاده‌سازی کنید.
5. پیش از Pull Request فرمان **npm run check** را اجرا کنید.
6. Pull Request را با توضیح روشن و تصویر تغییرات رابط کاربری ارسال کنید.

## اصول پروژه

- رابط فارسی و RTL باید در اندازه‌های موبایل و دسکتاپ حفظ شود.
- منبع داده جدید باید داخل ایران قابل دسترس و در **docs/DATA_SOURCES.md** مستند شود.
- داده یا قیمت بازسازی‌شده باید به‌وضوح از داده زنده تفکیک شود.
- کلید API، token یا فایل محیطی نباید commit شود.
- تغییرات رابط کاربری باید keyboard-accessible باشند.
